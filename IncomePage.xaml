<Page x:Class="GymManagementSystem.IncomePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:GymManagementSystem"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="IncomePage">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Image Source="Images/back.png" Grid.Row="0" Width="30" Height="30" 
               HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10"
               MouseLeftButtonDown="Back_Click"/>
        <StackPanel Grid.Row="1">
            <Label x:Name="TotalIncomeLabel" Content="Today Income: 0 EGP" FontWeight="Bold" Margin="10"/>
            <ComboBox x:Name="IncomeFilterComboBox" Width="200" Margin="10" SelectionChanged="IncomeFilterComboBox_SelectionChanged">
                <ComboBoxItem Content="Today"/>
                <ComboBoxItem Content="Yesterday"/>
                <ComboBoxItem Content="Last Week"/>
                <ComboBoxItem Content="Last Month"/>
            </ComboBox>
            <DataGrid x:Name="IncomeDataGrid" AutoGenerateColumns="False" IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" />
                    <DataGridTextColumn Header="Phone" Binding="{Binding Phone}" />
                    <DataGridTextColumn Header="Date" Binding="{Binding Date}" />
                    <DataGridTextColumn Header="Time" Binding="{Binding Time}" />
                    <DataGridTextColumn Header="Amount" Binding="{Binding Amount}" />
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </Grid>
</Page>
